# Copy this file to `.env` in the shared infrastructure directory.
# Never commit the resulting `.env` file.

LETSENCRYPT_EMAIL=ops@example.com

# Generate both passwords with `openssl rand -hex 32`.
MONGO_ROOT_USERNAME=cms_root
MONGO_ROOT_PASSWORD=
MONGO_APP_USERNAME=cms_runtime
MONGO_APP_PASSWORD=
