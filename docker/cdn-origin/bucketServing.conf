# cdn-origin does NOT serve buckets publicly. This file is shipped only
# because @bernouy/cdn's `aliasesServers.conf` generator references it
# via `include`. It defines a no-op location that returns 444 for any
# request that lands here — defensive, in case an alias server block
# accidentally gets activated on the origin.

location / {
    return 444;
}
