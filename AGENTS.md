
packages/ rules :
- READ rules in [base.md](.agents/rules/base.md).

Each package may also carry its own `AGENTS.md` (e.g. `packages/core`,
`packages/runner-bun`, `packages/cms-control`, `packages/cms-blocs`) — read it
when working inside that package; it overrides the generic rules above.
</content>
