Major release: Mondial Relay Delivery is now a backend-only Source package. UI blocks moved to the Ulvia Collection. The Edge Function is deployed under a new versioned slug so upgrades can switch bindings without replacing the live function in place.
