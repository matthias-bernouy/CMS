Generate usable PDF labels for local simulated shipments while preserving seller-bound access and the existing local simulation gate. The major version is required because the previous package lacks comparable Edge Function HTTP metadata; carrier HTTP contracts and database schemas are unchanged.
