{
    "type": "function",
    "function": {
        "id": "pauseNewsletterBroadcast",
        "method": "POST",
        "access": {
            "mode": "admin"
        },
        "meta": {
            "name": "Pause newsletter broadcast",
            "description": "Pause one running or scheduled broadcast campaign."
        },
        "input": {
            "body": {
                "type": "object",
                "properties": {
                    "campaignId": {
                        "type": "string"
                    }
                },
                "required": [
                    "campaignId"
                ]
            }
        },
        "steps": [
            {
                "id": "campaign",
                "call": {
                    "source": "{{answers.id}}-broadcast",
                    "endpoint": "pauseCampaign",
                    "params": {
                        "id": "$input.body.campaignId"
                    },
                    "body": {}
                }
            }
        ],
        "return": {
            "status": 200,
            "body": "$steps.campaign"
        }
    },
    "contractVersion": "1.0.0"
}
