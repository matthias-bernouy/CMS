Accept Mondial Relay provider versions 1 and 2 under the unchanged relay-points endpoint contract. Initialize optional checkout account email through its value attribute before the native input component is registered, preserving asynchronous binding and browser form state.
