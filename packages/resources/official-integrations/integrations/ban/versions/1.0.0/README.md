# Base Adresse Nationale Integration

This resource stores the official CmsCore source contract for the public French
Base Adresse Nationale geocoding API.

## Files

- `sources/ban.source.json`: public address search and reverse-geocoding source
  contract.

## CMS Installation

Import `definition.json` with kind `ban`. The definition installs the public
BAN source contract without writing secrets.
