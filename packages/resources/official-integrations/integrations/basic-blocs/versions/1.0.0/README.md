# Basic Blocs 1.0.0

Small neutral blocs intended as official UI building blocks.

This first version is deliberately minimal. It exists to validate the official
bloc pack format before integration dependency and override flows are added.
