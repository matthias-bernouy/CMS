{
    "name": "@bernouy/mt-cms-control",
    "version": "0.0.1",
    "description": "CMS Control Module for Modular CMS, built on Web Components with an inline visual editing system.",
    "license": "UNLICENSED",
    "type": "module",
    "main": "./src/index.ts",
    "exports": {
        ".": "./src/index.ts"
    },
    "scripts": {
        "build": "bun run build.ts",
        "typecheck": "bun x tsc --noEmit",
        "test": "bun test",
        "prepublishOnly": "bun run build"
    },
    "devDependencies": {
        "typescript": "^5.9.3"
    },
    "peerDependencies": {
        "typescript": "^5.9.3",
        "mongodb": "^7.2.0"
    },
    "dependencies": {
        "@bernouy/core":           "workspace:*",
        "@bernouy/runner-bun":     "workspace:*",
        "@bernouy/auth-keycloak":  "workspace:*",
        "@bernouy/auth-composite": "workspace:*",
        "@bernouy/cdn-buckets":    "workspace:*",
        "@bernouy/cms":            "workspace:*"
    },
    "engines": {
        "bun": ">=1.3.0"
    }
}
