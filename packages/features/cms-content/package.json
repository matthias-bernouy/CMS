{
    "name": "@bernouy/cms-content",
    "version": "0.1.0",
    "description": "The CMS content aggregate behind `CmsRepository` — pages, blocs, templates and settings (TSystem: site, editor shell, security extras, roles config), with in-memory and Mongo (`./mongo`) implementations, the theme style endpoint under `http/`, cache key builders and content utils. One package because it is ONE consistency domain: entities are copied, provisioned and rendered together.",
    "license": "UNLICENSED",
    "type": "module",
    "main": "./src/exports/index.ts",
    "exports": {
        ".": "./src/exports/index.ts",
        "./editor": "./src/exports/editor.ts",
        "./mongo": "./src/exports/mongo.ts"
    },
    "dependencies": {
        "@bernouy/http-runner": "workspace:*",
        "linkedom": "^0.18.12",
        "markdown-it": "^14.3.0"
    },
    "devDependencies": {
        "@types/markdown-it": "^14.1.2"
    },
    "peerDependencies": {
        "mongodb": "^7.2.0",
        "typescript": "^5.9.3"
    }
}
