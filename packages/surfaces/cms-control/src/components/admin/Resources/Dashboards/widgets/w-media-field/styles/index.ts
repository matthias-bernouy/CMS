import fieldCss from "./field.css" with { type: "text" };
import previewLayoutCss from "./preview-layout.css" with { type: "text" };
import previewMediaCss from "./preview-media.css" with { type: "text" };

export default [fieldCss, previewLayoutCss, previewMediaCss].join("\n");
